describe("Components", function(){

	describe("#responseListener", function(){

		it('should serailize data with different methods (HTML, JSON, XML ...)');

		it('should load user serailizers');

		it('should be acessible from the controller');

	});

});